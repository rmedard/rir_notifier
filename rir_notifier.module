<?php
/**
* @file
* A description of what your module does.
*/

use Drupal\Core\Entity\EntityInterface;
use Drupal\webform\WebformSubmissionInterface;

/**
 * Implements hook_page_attachments().
 */
function rir_notifier_page_attachments(array &$attachments) {
  if (Drupal::currentUser()->isAnonymous()){
    $attachments['#attached']['library'][] = 'rir_notifier/rir_scripts';
    $attachments['#attached']['library'][] = 'core/drupal.dialog.ajax';
  }
}

/**
 * Implements hook_theme().
 */
function rir_notifier_theme($existing, $type, $theme, $path) {

  return [
    'rir_subscribe_search' => [
      'variables' => [
        'button' => t('Subscribe'),
      ]
    ]
  ];
}

/**
 * Implements hook_ENTITY_TYPE_insert().
 */
function rir_notifier_webform_submission_insert(WebformSubmissionInterface $entity) {
  if ($entity->getWebform()->id() == 'notification_subscription'){
    kint($entity->getData());
    die();
    $districts = $entity->getData('notif_districts');
    $notifType = $entity->getData('notif_advert_type');
    $nbrRooms = $entity->getData('notif_nbr_rooms');
    $propertyType = $entity->getData('notif_property_type');
    $price = $entity->getData('notif_price');
    $referenceNbr = getRequestCategoryReference($districts, $notifType, $nbrRooms, $propertyType, $price);
    $requestCategoryCount = Drupal::entityQuery('node')
      ->condition('type', 'details_request_category')
      ->condition('field_dr_reference', $referenceNbr)
      ->count();
    // TODO Continue here... Put logic in a queue worker
  }
}